datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Analysis {
  id           String   @id @default(uuid())
  imageBase64  String
  severity     String
  accuracy     Float
  commentaries String
  createdAt    DateTime @default(now())
}
